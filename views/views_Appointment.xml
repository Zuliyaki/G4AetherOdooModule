<odoo>
    <data>
        <!-- explicit list view definition -->
        <!--
        <record model="ir.ui.view" id="aether.list">
          <field name="name">aether list</field>
          <field name="model">aether.aether</field>
          <field name="arch" type="xml">
            <tree>
              <field name="name"/>
              <field name="value"/>
              <field name="value2"/>
            </tree>
          </field>
        </record>
        -->

        <!-- actions opening views on models -->

        <record model="ir.actions.act_window" id="aether.action_window_1">
            <field name="name">aether window</field>
            <field name="res_model">aether.aether</field>
            <field name="view_mode">tree,form</field>
        </record>

        <!-- server action to the one above -->
        <!--
        <record model="ir.actions.server" id="aether.action_server">
          <field name="name">aether server</field>
          <field name="model_id" ref="model_aether_aether"/>
          <field name="code">
            action = {
              "type": "ir.actions.act_window",
              "view_mode": "tree,form",
              "res_model": self._name,
            }
          </field>
        </record>
        -->

        <!-- actions -->
  
        <menuitem name="Appointment" 
                  id="aether.menu_1_list" 
                  parent="aether.menu_1"
                  action="aether.action_window_1"/>
        <!--
        <menuitem name="Server to list" id="aether" parent="aether.menu_2"
                  action="aether.action_server"/>
        -->
    </data>
</odoo>